package main

import "os"

type Stringer interface{ String() string }

type Thing struct{ n int }

func (t Thing) String() string { return "thing" }

var sink Stringer

func main() {
	sink = Thing{n: len(os.Args)}
	os.Exit(len(sink.String()) - 5)
}
