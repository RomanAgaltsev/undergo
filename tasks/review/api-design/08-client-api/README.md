# Drill: 08-client-api

- **Category:** C6 — API & interface design (see `../../../../rubric/bug-taxonomy.md`)
- **Tier:** multi-bug

## PR description

> Adds an API `Client` with a `Do` method (guarded by a mutex), a `Timeout` accessor, and a
> `New` constructor. Several API-design problems are hiding here — find as many as you can.

## Files to review

- `drill.go`

## Your task

Review against `../../../../rubric/review-rubric.md`; write findings in the
`../../../../rubric/submission-format.md` format (one line per finding). **Do not run `undergo reveal review/api-design/08-client-api` until you submit.**
