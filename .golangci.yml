version: 2

linters:
  default: standard
  enable:
    - bodyclose
    - errorlint
    - gocritic
    - misspell
    - revive
    - unconvert
  exclusions:
    paths:
      - work
      - _solution
      - vendor

formatters:
  enable:
    - gofumpt
    - goimports
