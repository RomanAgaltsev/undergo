schema: 1
id: edges/07-init-order
title: "Before main, in what order"
mode: predict
track: edges
difficulty: 4
estimate: 40m
tags: [edges, initialisation, init, package-init, dependency-order]
predict:
  slots:
    [
      first_line,
      main_first_var,
      init_runs_after_its_package_vars,
      full_order,
    ]
inspired_by: "radar candidate — package initialisation order became a specified algorithm rather than an implementation detail"
