# undergo

Go katas beneath the surface — layout, allocation, escape, reflection, generics,
iterators, weak pointers, GC, the scheduler, the memory model, the compiler, assembly.

Status: under construction. See `ROADMAP.md`.
