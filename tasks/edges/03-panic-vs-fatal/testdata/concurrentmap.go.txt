package main

import "fmt"

func main() {
	defer func() {
		if r := recover(); r != nil {
			fmt.Println("RECOVERED")
		}
	}()
	m := map[int]int{}
	go func() {
		for i := 0; ; i++ {
			m[i] = i
		}
	}()
	for i := 0; ; i++ {
		_ = m[i]
	}
}
