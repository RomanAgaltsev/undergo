# Drill: 03-buffer-api

- **Category:** C6 — API & interface design (see `../../../../rubric/bug-taxonomy.md`)
- **Tier:** multi-bug

## PR description

> Adds a `Buffer` type that accumulates bytes with `Append`, reports `Len`, and has an `Open`
> constructor that loads a file. A few API-design choices are questionable — find as many as
> you can.

## Files to review

- `drill.go`

## Your task

Review against `../../../../rubric/review-rubric.md`; write findings in the
`../../../../rubric/submission-format.md` format (one line per finding). **Do not run `undergo reveal review/api-design/03-buffer-api` until you submit.**
