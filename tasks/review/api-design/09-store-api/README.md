# Drill: 09-store-api

- **Category:** C6 — API & interface design (see `../../../../rubric/bug-taxonomy.md`)
- **Tier:** multi-bug

## PR description

> A `Store` of records with a `NewStore` constructor, a `GetName` accessor, a `Find` lookup, and
> an `Update` mutator. Several API-design problems are hiding here — find as many as you can.

## Files to review

- `drill.go`

## Your task

Review against `../../../../rubric/review-rubric.md`; write findings in the
`../../../../rubric/submission-format.md` format (one line per finding). **Do not run `undergo reveal review/api-design/09-store-api` until you submit.**
